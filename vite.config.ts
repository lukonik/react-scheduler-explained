import { defineConfig } from "vite-plus";

export default defineConfig({
  root: "./playground",
  pack: {
    dts: {
      tsgo: true,
    },
    exports: true,
  },
  lint: {
    options: {
      typeAware: true,
      typeCheck: true,
    },
  },
  fmt: {},
});
